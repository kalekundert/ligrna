run*.sh
analyze*.sh

scripts/
    libraries for run/analyze scripts, require_commit
inputs/
    input files that are version controlled.
database/
    input files that aren't version controlled.
results/
    Populated by run*.sh scripts, not version controlled.
figures/
    Populated by analyze*.sh scripts, not version controlled.
